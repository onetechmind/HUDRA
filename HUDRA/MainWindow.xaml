<Window
    x:Class="HUDRA.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HUDRA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border x:Name="MainBorder" CornerRadius="20" Background="Transparent" >
        <Grid x:Name="LayoutRoot">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- TDP Picker centered in the main area -->
            <StackPanel Grid.Row="0" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Top" 
                    Spacing="10"
                    Margin="0,40,0,0">
                <!-- TDP Label -->
                <TextBlock Text="TDP" 
                       FontSize="14" 
                       Foreground="#CCFFFFFF"
                       HorizontalAlignment="Center"/>

                <!-- Custom TDP Picker Wheel - restore fixed width -->
                <Border x:Name="TdpPickerBorder"
                        Background="#22FFFFFF" 
                    CornerRadius="12" 
                    Padding="20,0"
                    Width="280">
                    <Grid Height="80">
                        <ScrollViewer x:Name="TdpScrollViewer"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Disabled"
                                  HorizontalScrollMode="Enabled"
                                  VerticalScrollMode="Disabled"
                                  ZoomMode="Disabled"
                                  ManipulationMode="TranslateX"
                                  ViewChanged="TdpScrollViewer_ViewChanged"
                                  Padding="0,20">
                            <StackPanel x:Name="NumbersPanel" 
                                    Orientation="Horizontal" 
                                    Spacing="15"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
                
                <!-- Mute Button - restore auto width -->
                <Button x:Name="MuteButton"
            Content="Toggle Mute"
            Click="MuteButton_Click"
            Width="Auto"
            Background="#4C4C4C"
            Foreground="White"
            CornerRadius="8"/>
            </StackPanel>

            <!-- Current TDP Display at the bottom -->
            <TextBlock Grid.Row="1"
                   Text="{x:Bind CurrentTdpDisplayText, Mode=OneWay}"
                   FontSize="12" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        </Grid>
    </Border>
</Window>
<UserControl
    x:Class="HUDRA.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="BackButton"
                FontFamily="Segoe MDL2 Assets"
                FontSize="25"
                Content="&#xE80F;"
                Width="50" Height="50"
                Background="Transparent" Foreground="White"
                CornerRadius="16" BorderThickness="0"
                HorizontalAlignment="Left" VerticalAlignment="Top"
                Margin="10" x:FieldModifier="public"/>

        <TextBlock Text="SETTINGS" Grid.Row="1"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="20" Margin="0,0,0,20"
                   Foreground="White" FontFamily="Cascadia Code"/>

        <StackPanel Grid.Row="2" Spacing="15" Padding="0" MaxWidth="280">

            <!-- TDP Correction setting -->
            <Border Background="#22FFFFFF" 
            CornerRadius="12" 
            Padding="20,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Sticky TDP" 
                       Style="{StaticResource SettingsLabelStyle}"
                       Grid.Column="0"/>

                    <ToggleSwitch x:Name="TdpCorrectionToggle"
                          Grid.Column="1"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Right"
                          Margin="10,0,0,0"
                          Toggled="TdpCorrectionToggle_Toggled"/>
                </Grid>

            </Border>

        </StackPanel>
    </Grid>
</UserControl>
    
<Window
    x:Class="HUDRA.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HUDRA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel x:Name="LayoutRoot" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">

        <TextBlock Text="Ryzen TDP Control" HorizontalAlignment="Center" Style="{ThemeResource TitleTextBlockStyle}"/>

        <Slider x:Name="TdpSlider" 
                Minimum="5" 
                Maximum="35" 
                StepFrequency="1" 
                Width="300" 
                Header="TDP Limit (Watts)"/>

        <TextBlock x:Name="CurrentTdpStatusText"
                   Text="{Binding CurrentTdpDisplayText}"
                   HorizontalAlignment="Center"
                   Style="{ThemeResource SubtitleTextBlockStyle}"
                   Margin="0,10,0,0"/>

        <Button x:Name="SetTdpButton" 
                Content="Apply TDP" 
                HorizontalAlignment="Center" 
                Click="SetTdpButton_Click"/>

    </StackPanel>
</Window>
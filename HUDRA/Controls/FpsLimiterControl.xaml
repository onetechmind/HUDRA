<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="HUDRA.Controls.FpsLimiterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ap="using:HUDRA.AttachedProperties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:HUDRA.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    ap:GamepadNavigation.IsEnabled="True"
    ap:GamepadNavigation.NavigationGroup="MainControls"
    ap:GamepadNavigation.NavigationOrder="4"
    mc:Ignorable="d">

    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!--  NumberBox styled to match HUDRA design language  -->
        <Style x:Key="HudraNumberBoxStyle" TargetType="NumberBox">
            <Setter Property="Background" Value="#22FFFFFF" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontFamily" Value="Cascadia Code" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Height" Value="48" />
            <Setter Property="SpinButtonPlacementMode" Value="Compact" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Minimum" Value="0" />
            <Setter Property="SmallChange" Value="1" />
            <Setter Property="LargeChange" Value="5" />
            <Setter Property="ValidationMode" Value="InvalidInputOverwritten" />
            <Setter Property="AcceptsExpression" Value="False" />
        </Style>

        <Style x:Key="HudraHyperlinkButtonStyle" TargetType="HyperlinkButton">
            <Setter Property="Foreground" Value="DarkViolet" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="HyperlinkButton">
                        <ContentPresenter
                            x:Name="ContentPresenter"
                            Padding="{TemplateBinding Padding}"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                            AutomationProperties.AccessibilityView="Raw"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            ContentTransitions="{TemplateBinding ContentTransitions}"
                            Foreground="DarkViolet">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="MediumOrchid" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Plum" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </ContentPresenter>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel Spacing="8">
        <!--  Labels  -->
        <Grid Width="280">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                HorizontalAlignment="Center"
                FontSize="14"
                FontWeight="SemiBold"
                Foreground="#CCFFFFFF"
                IsTabStop="False"
                Text="FPS Limit" />

            <TextBlock
                Grid.Column="2"
                HorizontalAlignment="Center"
                FontSize="14"
                FontWeight="SemiBold"
                Foreground="#CCFFFFFF"
                IsTabStop="False"
                Text="HDR" />
        </Grid>

        <!--  Controls Row  -->
        <Grid Width="280">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <!--  FPS NumberBox with focus border  -->
            <Border
                x:Name="FpsFocusBorder"
                Grid.Column="0"
                BorderBrush="{x:Bind FpsFocusBrush, Mode=OneWay}"
                BorderThickness="2"
                CornerRadius="12">
                <NumberBox
                    x:Name="FpsNumberBox"
                    IsEnabled="{Binding IsRtssInstalled, Mode=OneWay}"
                    Maximum="{x:Bind MaxFps, Mode=OneWay}"
                    Style="{StaticResource HudraNumberBoxStyle}"
                    ValueChanged="OnFpsValueChanged" />
            </Border>

            <!--  HDR Toggle with focus border  -->
            <Border
                x:Name="HdrFocusBorder"
                Grid.Column="2"
                Padding="25,0"
                Background="#22FFFFFF"
                BorderBrush="{x:Bind HdrFocusBrush, Mode=OneWay}"
                BorderThickness="2"
                CornerRadius="12">
                <ToggleSwitch
                    x:Name="HdrToggle"
                    FontSize="13"
                    IsEnabled="{x:Bind IsHdrSupported, Mode=OneWay}"
                    OffContent="OFF"
                    OnContent="ON"
                    Toggled="OnHdrToggled" />
            </Border>
        </Grid>

        <!--  "0 = Unlimited" hint  -->
        <TextBlock
            Width="280"
            HorizontalAlignment="Left"
            FontFamily="Cascadia Code"
            FontSize="11"
            Foreground="#66FFFFFF"
            IsTabStop="False"
            Text="0 = Unlimited  ·  max = refresh rate" />

        <!--  RTSS Not Found Message  -->
        <StackPanel Spacing="8" Visibility="{Binding IsRtssNotInstalled, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="14"
                FontWeight="SemiBold"
                Foreground="DarkViolet"
                Text="RTSS Required" />
            <StackPanel
                HorizontalAlignment="Center"
                Orientation="Horizontal"
                Spacing="4">
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="#99FFFFFF"
                    Text="Install" />
                <HyperlinkButton
                    x:Name="MsiAfterburnerLink"
                    Padding="0"
                    Click="OnMsiAfterburnerLinkClick"
                    Content="MSI Afterburner"
                    FontSize="11"
                    Style="{StaticResource HudraHyperlinkButtonStyle}" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="#99FFFFFF"
                    Text="or" />
                <HyperlinkButton
                    x:Name="RtssLink"
                    Padding="0"
                    Click="OnRtssLinkClick"
                    Content="RTSS"
                    FontSize="11"
                    Style="{StaticResource HudraHyperlinkButtonStyle}" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="#99FFFFFF"
                    Text="for FPS Limiting." />
            </StackPanel>
        </StackPanel>
    </StackPanel>

</UserControl>

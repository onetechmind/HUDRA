<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="HUDRA.Controls.AmdFeaturesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ap="using:HUDRA.AttachedProperties"
    xmlns:controls="using:HUDRA.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    ap:GamepadNavigation.IsEnabled="True"
    ap:GamepadNavigation.NavigationGroup="MainControls"
    ap:GamepadNavigation.NavigationOrder="1"
    mc:Ignorable="d">

    <StackPanel Spacing="0">
        <!--  RSR Toggle  -->
        <Border
            Margin="0"
            Padding="15"
            CornerRadius="12">
            <StackPanel Spacing="10">
                <TextBlock
                    Padding="5,0,5,0"
                    FontSize="14"
                    Text="Direct control of common AMD Adrenalin features. "
                    TextWrapping="Wrap" />
                <Border
                    x:Name="RsrToggleBorder"
                    Padding="10,5,10,5"
                    Background="#22FFFFFF"
                    BorderBrush="{x:Bind RsrToggleFocusBrush, Mode=OneWay}"
                    BorderThickness="2"
                    CornerRadius="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            FontFamily="Cascadia Code"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Text="Radeon Super&#x0a;Resolution" />
                        <ToggleSwitch
                            x:Name="RsrToggle"
                            Grid.Column="1"
                            IsOn="{Binding RsrEnabled, Mode=TwoWay}" />
                    </Grid>
                </Border>

            </StackPanel>
        </Border>

        <!--  RSR Sharpness Slider  -->
        <Border Padding="10,0,10,5" CornerRadius="12">
            <StackPanel Spacing="10">
                <Border
                    x:Name="SharpnessSliderBorder"
                    Margin="5"
                    Padding="10,20,10,10"
                    Background="#22FFFFFF"
                    BorderBrush="{x:Bind SharpnessSliderFocusBrush, Mode=OneWay}"
                    BorderThickness="2"
                    CornerRadius="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            FontFamily="Cascadia Code"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Text="RSR Sharpness" />
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="1"
                            FontFamily="Cascadia Code"
                            FontSize="12"
                            Text="{Binding RsrSharpness, Mode=OneWay}" />
                        <Slider
                            x:Name="SharpnessSlider"
                            Grid.Row="1"
                            Grid.ColumnSpan="2"
                            IsEnabled="{Binding RsrEnabled, Mode=OneWay}"
                            Maximum="100"
                            Minimum="0"
                            Value="{Binding RsrSharpness, Mode=TwoWay}" />
                    </Grid>
                </Border>

                <!--  Sharpness Description  -->
                <TextBlock
                    Padding="10,0,10,0"
                    Style="{StaticResource SettingsDescriptionStyle}"
                    Text="AMD’s upscaling boosts lower-resolution images to your display’s full resolution."
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <!--  AFMF Toggle  -->
        <Border
            Margin="0"
            Padding="15,5,15,5"
            CornerRadius="12">
            <StackPanel Spacing="10">
                <Border
                    x:Name="AfmfToggleBorder"
                    Padding="10,5,10,5"
                    Background="#22FFFFFF"
                    BorderBrush="{x:Bind AfmfToggleFocusBrush, Mode=OneWay}"
                    BorderThickness="2"
                    CornerRadius="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            FontFamily="Cascadia Code"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Text="Fluid Motion Frames" />
                        <ToggleSwitch
                            x:Name="AfmfToggle"
                            Grid.Column="1"
                            IsOn="{Binding AfmfEnabled, Mode=TwoWay}" />
                    </Grid>
                </Border>

                <!--  AFMF Description  -->
                <TextBlock
                    Padding="5,0,5,0"
                    Style="{StaticResource SettingsDescriptionStyle}"
                    Text="AMD's frame generation technology adds extra frames for smoother motion."
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <!--  Anti-Lag Toggle  -->
        <Border
            Margin="0"
            Padding="15,5,15,5"
            CornerRadius="12">
            <StackPanel Spacing="10">
                <Border
                    x:Name="AntiLagToggleBorder"
                    Padding="10,5,10,5"
                    Background="#22FFFFFF"
                    BorderBrush="{x:Bind AntiLagToggleFocusBrush, Mode=OneWay}"
                    BorderThickness="2"
                    CornerRadius="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            FontFamily="Cascadia Code"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Text="Radeon Anti-Lag" />
                        <ToggleSwitch
                            x:Name="AntiLagToggle"
                            Grid.Column="1"
                            IsOn="{Binding AntiLagEnabled, Mode=TwoWay}" />
                    </Grid>
                </Border>

                <!--  Anti-Lag Description  -->
                <TextBlock
                    Padding="5,0,5,0"
                    Style="{StaticResource SettingsDescriptionStyle}"
                    Text="AMD's latency reduction technology for improved responsiveness."
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>

<Window
    x:Class="HUDRA.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HUDRA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border x:Name="MainBorder" CornerRadius="12" Background="#22000000">
        <Grid x:Name="LayoutRoot">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
                <!-- Your existing content stays the same -->
                <TextBlock Text="{x:Bind CurrentTdpDisplayText, Mode=OneWay}"
                           FontSize="20" HorizontalAlignment="Center"/>

                <!-- Custom TDP Picker Wheel -->
                <Border Background="#22FFFFFF" 
                        CornerRadius="12" 
                        Padding="20,15"
                        Width="280">
                    <!-- Rest of your picker content stays the same -->
                    <StackPanel Spacing="15">
                        <TextBlock Text="TDP (Watts)" 
                                   FontSize="14" 
                                   Foreground="#CCFFFFFF"
                                   HorizontalAlignment="Center"/>

                        <Grid Height="80">
                            <ScrollViewer x:Name="TdpScrollViewer"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Disabled"
                                          HorizontalScrollMode="Enabled"
                                          VerticalScrollMode="Disabled"
                                          ZoomMode="Disabled"
                                          ViewChanged="TdpScrollViewer_ViewChanged"
                                          Padding="0,20">
                                <StackPanel x:Name="NumbersPanel" 
                                            Orientation="Horizontal" 
                                            Spacing="15"/>
                            </ScrollViewer>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</Window>